

notifications:
  email:
    recipients:
      - humberto.a.sanchez.ii@gmail.com
    on_success: change # default: change
    on_failure: always # default: always

language: python

dist: noble

cache: pip

services:
  - xvfb

python:
  - "3.13"

install:
  - echo '***************************************************************'
  - uname -a
  - sudo apt update
  - sudo apt install libnotify-dev
  - sudo apt install libgtk-3-dev
  - sudo apt-get install -y -V libtiff-dev
  - sudo apt-get install -y  libsdl2-dev
  - echo '***************************************************************'
  - GLOBAL=`python --version`
  - echo $GLOBAL
  - PY_VERSION=${GLOBAL#* }
  - pyenv global ${PY_VERSION}
  - pip install pip==26.0.1
  - pip install wheel==0.44.0
  - pip install setuptools==70.1.1
  - pip install six==1.17.0
  - pip install attrdict3==2.0.2  
  - pip install -v -f https://extras.wxpython.org/wxPython4/extras/linux/gtk3/ubuntu-24.04  wxPython==4.2.4 -c constraints.txt
  - pip install html-testRunner==1.2.1
  - pip install codeallybasic==1.30.1
  - pip install umlmodel==3.4.1
  - pip install umlshapes==2.3.1
  - pip install untangle==1.2.1
  - pip install umlio==0.7.1
  - pip install codeallyadvanced==2.3.0
  - pip install pyorthogonalrouting==2.0.1
  - pip install pyforcedirectedlayout==2.0.0
  - pip install orthogonal==1.3.3
  - pip install antlr4-python3-runtime==4.13.2
  - pip install networkx==3.6.1
  - pip install numpy==2.4.4
  - pip install scipy==1.17.1



# command to run tests
script:
  - pip install buildlackey
  - unittests -v loud
